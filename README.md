# WebBankirQaTest

Для запуска необходимо передать путь к исполняемому файлу ChromeDriver через переменную окружения CHROME_DRIVER_PATH.
С помощью команды ./gradlew test запустить тест.

Либо через Идею в конфигурации запуска добавить в Environment variables путь до chromedriver


Задание 4. Выполнен Вариант B: Параметризация и Вариант C: Анализ производительности

Если тесты идут последовательно, так что первое, что я бы сделал, это включил параллелизацию junit, общее время прогона снизится в разыю
Также можно только один раз инициализировать драйвер на весь прогон, а после каждого теста чистить куки и локал сторадж,это даст экономию в несколько секунд на каждом тесте, не будет каждый раз стартовать новый браузер.
Запускать тесты в headless режиме,отключить в ChromeOptions все ненужное